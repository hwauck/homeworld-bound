using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// this script allows me to know at any time whether the GameObject's attached
// MeshCollider is supposed to be marked as convex or not. This allows me to reset the GameObject's 
// MeshColliders to the correct convexity. You only need to attach this script to child objects of 
// the starting part that have a MeshCollider with "Convex" checked
public class Convexity : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
